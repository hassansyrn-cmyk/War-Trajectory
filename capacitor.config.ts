import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.hasan.wartrajectory",
  appName: "War Trajectory",
  webDir: "dist",
  backgroundColor: "#0b1220",
  android: {
    backgroundColor: "#0b1220",
  },
};

export default config;
